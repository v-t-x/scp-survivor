# v1.6.0「收容区焕新」集成与发布实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 UI/Art 精确截止提交 `31d8a24` 集成到最新稳定 `main`，恢复 v1.5 的无输入冲刺语义，完成 v1.6 版本、文档、许可和素材登记修订，并把候选版本推进到自动化、源码审查、浏览器 smoke 与项目所有者验收门禁。

**Architecture:** 设计和计划先保留在本地 `main`，随后从刷新后的 `main` 创建 `codex/release-v1.6` 独立 worktree，并以一次 `--no-ff` merge 引入精确 cutoff。所有修复和发布准备只发生在候选分支；运行时回归、版本/CI、许可登记、玩家文案和权威文档分别形成可审查提交。公开 push、PR、merge、tag 与 Release 是后置授权阶段，不能由本地自动化或 Agent 视觉 smoke 自动放行。

**Tech Stack:** Windows PowerShell、Git worktree、GitHub CLI、Phaser 3.90、Vite 7、JavaScript ES Modules、Node.js test runner、Python/Pillow 像素测试、Markdown。

## Global Constraints

- UI/Art 唯一集成边界是 `31d8a240963b98f994d25c96414c9802a268ba0e`。
- 不得把 `da8b2354d5ab8344c2a873219d6bee992eb914d4`、`7767846b5007dc96b3cc03ba9832e57a3a9f0cbe`、`4a5512ffcdccff37c72541c58cd82f56594db468`、`68c23bc67134fe2e34a086217d639496320f8f25` 四个 cutoff 后角色规划提交纳入候选分支祖先链。
- 候选分支固定为 `codex/release-v1.6`，worktree 固定为 `C:\scp-survivor-workspaces\releases\v1.6`；创建前必须调用 `superpowers:using-git-worktrees`。
- 集成使用一次 `--no-ff` merge；不得 cherry-pick 整条历史、squash、rebase、force-push 或重写既有分支与标签。
- `.superpowers/`、`dist/`、`node_modules/`、本机生成源和临时 smoke 文件不得提交。
- v1.6 无输入冲刺保持 v1.5 语义：没有 WASD 输入时跟随成功提交的当前攻击朝向；移动方向记忆只服务表现层，不替代冲刺 fallback。
- 不修改数值、刷怪、AI、存档、奖励、胜负条件和六分钟结构；不引入橘色路线、120 帧角色路线、Electron 或 Windows 包。
- `package.json` 与 lockfile 根版本统一为 `1.6.0`，`DEBUG_MODE=false`。
- 中文和英文 README 保持事实一致；公开标题为 `v1.6.0 — 收容区焕新`，英文对应名称为 `v1.6.0 — Containment Zone Reforged`。
- 许可文件结构只是待 Level 2 审查确认的候选方案；首次公开 push 前必须生成外部 GPT 审查包，仍有实质疑问时不得由 Agent 自行宣布合规。
- 自动化、源码审查和 Agent smoke 不能代替项目所有者视觉/试玩与许可验收。
- 用户已授权计划完成后直接开始本地实施；这不取消规格中对公开 push、PR、merge、tag、Release 和项目所有者验收的独立门禁。

## File Responsibility Map

- `src/scene/systems.js`：恢复无输入冲刺 fallback，保留表现层移动方向记忆。
- `test/attack-facing.test.js`：真实覆盖“上移 → 向右霰弹枪攻击 → 无输入向右冲刺”。
- `package.json`、`package-lock.json`：v1.6 元数据、标准测试脚本和混合许可入口。
- `.github/workflows/ci.yml`：通过 `npm test` 后构建。
- `LICENSE`、`LICENSES/MIT.txt`、`LICENSES/CC-BY-SA-3.0.txt`：顶层许可总览与完整许可文本。
- `ATTRIBUTION.md`：SCP Wiki、SCP-049 和项目生成素材的公开署名。
- `docs/art/asset-register.md`：53 个生产 PNG、51 个运行时素材、2 个非运行时保留素材及来源边界。
- `README.md`、`README.en.md`、`CHANGELOG.md`、`docs/releases/v1.6.0.md`：面向玩家的版本入口和 Release 文案源。
- `docs/design.md`、`docs/project-status.md`、`docs/README.md`、`docs/licensing-and-commercialization.md`：`main` 当前事实、状态、文档地图和许可政策。
- `docs/superpowers/plans/2026-07-18-v1.6-integration-and-release.md`：任务证据和完成复选框。

---

### Task 1: 创建隔离候选并精确合并 UI/Art cutoff

**Files:**
- Inspect only: `C:\scp-survivor`
- Create worktree: `C:\scp-survivor-workspaces\releases\v1.6`
- Git history only: merge commit on `codex/release-v1.6`

**Interfaces:**
- Consumes: 本地 `main` 中已批准的设计和本计划；UI/Art cutoff `31d8a24`。
- Produces: 只包含 cutoff 完整实现、不包含后 4 个规划提交的候选分支。

- [ ] **Step 1: 刷新远端并验证本地 main 边界**

在 `C:\scp-survivor` 运行：

```powershell
git fetch origin --prune
git status --short --branch
git merge-base --is-ancestor origin/main main
if ($LASTEXITCODE -ne 0) { throw 'origin/main 不是本地 main 的祖先，停止创建候选分支' }
if (git status --porcelain) { throw 'main 工作树不干净，停止创建候选分支' }
git log --oneline --decorate -5 main
```

Expected: `main` 仅因已批准的 v1.6 设计/计划提交领先远端；工作树干净；不存在远端分叉。

- [ ] **Step 2: 验证 cutoff、共同祖先与排除提交**

```powershell
$cutoff = '31d8a240963b98f994d25c96414c9802a268ba0e'
$mergeBase = 'daeb3ed605f29a2d97a30458ee12109561dbaf6d'
$excluded = @(
  'da8b2354d5ab8344c2a873219d6bee992eb914d4',
  '7767846b5007dc96b3cc03ba9832e57a3a9f0cbe',
  '4a5512ffcdccff37c72541c58cd82f56594db468',
  '68c23bc67134fe2e34a086217d639496320f8f25'
)
git cat-file -e "$cutoff^{commit}"
if ($LASTEXITCODE -ne 0) { throw 'cutoff commit 不存在' }
if ((git merge-base main $cutoff) -ne $mergeBase) { throw '共同祖先已变化，停止' }
if ((git rev-list --count "$mergeBase..$cutoff") -ne '122') { throw 'cutoff 提交数已变化，停止' }
foreach ($commit in $excluded) { git cat-file -e "$commit^{commit}"; if ($LASTEXITCODE -ne 0) { throw "缺少排除提交 $commit" } }
```

Expected: cutoff、共同祖先、122 个实现提交和 4 个排除提交均精确匹配。

- [ ] **Step 3: 使用 worktree skill 创建候选分支**

先调用 `superpowers:using-git-worktrees`，再执行：

```powershell
if (Test-Path -LiteralPath 'C:\scp-survivor-workspaces\releases\v1.6') { throw '目标 worktree 路径已存在' }
if (git branch --list 'codex/release-v1.6') { throw '候选分支已存在' }
git worktree add -b codex/release-v1.6 'C:\scp-survivor-workspaces\releases\v1.6' main
git -C 'C:\scp-survivor-workspaces\releases\v1.6' status --short --branch
```

Expected: 新 worktree 位于指定路径，当前分支为 `codex/release-v1.6`，工作树干净。

- [ ] **Step 4: 合并精确 cutoff**

在候选 worktree 运行：

```powershell
git merge --no-ff 31d8a240963b98f994d25c96414c9802a268ba0e -m "merge: integrate v1.6 UI art vertical slice"
```

Expected: 创建一个双亲 merge commit；没有冲突。若出现冲突，不自动猜测，保留现场并停止该任务。

- [ ] **Step 5: 验证祖先、排除项与私有目录**

```powershell
$excluded = @(
  'da8b2354d5ab8344c2a873219d6bee992eb914d4',
  '7767846b5007dc96b3cc03ba9832e57a3a9f0cbe',
  '4a5512ffcdccff37c72541c58cd82f56594db468',
  '68c23bc67134fe2e34a086217d639496320f8f25'
)
git merge-base --is-ancestor 31d8a240963b98f994d25c96414c9802a268ba0e HEAD
if ($LASTEXITCODE -ne 0) { throw 'cutoff 未进入候选分支' }
foreach ($commit in $excluded) {
  git merge-base --is-ancestor $commit HEAD
  if ($LASTEXITCODE -eq 0) { throw "cutoff 后规划提交误入候选分支：$commit" }
}
if (Test-Path -LiteralPath '.superpowers') { throw '候选 worktree 意外包含 .superpowers' }
git show -s --format='%H %P %s' HEAD
git status --short --branch
```

Expected: cutoff 是祖先；4 个规划提交都不是祖先；merge commit 有两个 parent；工作树干净。

- [ ] **Step 6: 建立合并后自动化基线**

```powershell
npm.cmd ci
if ($LASTEXITCODE -ne 0) { throw 'npm ci 失败' }
node --test
if ($LASTEXITCODE -ne 0) { throw 'Node 测试失败' }
$python = (Get-Command python -ErrorAction SilentlyContinue).Source
if (-not $python -or $python -match '\\WindowsApps\\') {
  $python = Join-Path ([Environment]::GetFolderPath('UserProfile')) '.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
}
if (-not (Test-Path -LiteralPath $python)) { throw '找不到可执行的 Python runtime' }
& $python scripts/art/test_pixel_tools.py
if ($LASTEXITCODE -ne 0) { throw 'Python 像素测试失败' }
npm.cmd run build
if ($LASTEXITCODE -ne 0) { throw 'Vite 构建失败' }
git diff --check
if ($LASTEXITCODE -ne 0) { throw 'diff check 失败' }
```

Expected: 311/311 Node 测试通过；Python 像素测试通过；Vite 构建成功，仅允许记录既有的大 chunk warning；diff check 无输出。

---

### Task 2: 用 TDD 恢复 v1.5 无输入冲刺语义

**Files:**
- Modify: `test/attack-facing.test.js:80-116`
- Modify: `src/scene/systems.js:102-121`

**Interfaces:**
- Consumes: `attackWithShotgun()` 成功弹丸提交后写入的 `playerFacingAngle`；表现层保留的 `playerMovementFallbackAngle`。
- Produces: `tryStartDash()` 有输入时使用输入方向、无输入时使用当前攻击朝向。

- [ ] **Step 1: 把旧测试替换为真实失败序列**

用以下完整测试替换 `test/attack-facing.test.js` 的首个测试：

```js
test("no-input dash follows the facing committed by a successful shotgun attack", async () => {
  const { attackWithShotgun } = await loadMethods("attackWithShotgun");
  const { handlePlayerMovement, tryStartDash } = await loadSystemMethods(
    "handlePlayerMovement",
    "tryStartDash"
  );
  const velocity = [];
  const scene = {
    player: {
      x: 100,
      y: 100,
      body: { setVelocity(x, y) { velocity.push([x, y]); } }
    },
    playerFacingAngle: Math.PI,
    playerMovementFallbackAngle: 0,
    playerMoveSpeed: 100,
    moveSpeedBuffMultiplier: 1,
    elapsedSurvivalMs: 10,
    dashUntilMs: 0,
    dashReadyAtMs: 0,
    keys: {
      left: { isDown: false }, right: { isDown: false },
      up: { isDown: true }, down: { isDown: false }
    },
    isMissionActive: true,
    isGameOver: false,
    isLevelUpActive: false,
    playerInvulnerableUntilMs: 0,
    bossPhaseActive: false,
    findNearestEnemy: () => ({ x: 200, y: 100, active: true }),
    spawnPlayerProjectile({ angle }) { return { presentationAngle: angle }; },
    spawnDashTrail() {},
    playSound() {}
  };
  const shotgun = {
    isReloading: false,
    currentShells: 2,
    triggerRange: 120,
    range: 300,
    nextShotId: 1,
    pelletCount: 3,
    spreadDeg: 20,
    damage: 8,
    projectileSpeed: 220,
    reloadDurationMs: 900
  };

  handlePlayerMovement.call(scene);
  assert.deepEqual(velocity, [[0, -100]]);
  assert.equal(scene.playerMovementFallbackAngle, -Math.PI / 2);
  assert.equal(scene.playerFacingAngle, Math.PI, "movement must not overwrite committed attack-facing");

  scene.keys.up.isDown = false;
  assert.equal(attackWithShotgun.call(scene, shotgun), true);
  assert.equal(scene.playerFacingAngle, 0, "successful shotgun pellets commit the right-facing angle");
  assert.equal(scene.playerMovementFallbackAngle, -Math.PI / 2, "attack must not overwrite movement memory");

  tryStartDash.call(scene);
  assert.equal(scene.dashAngle, 0, "no-input dash must follow the committed attack-facing");
  assert.equal(scene.playerFacingAngle, 0);
});
```

- [ ] **Step 2: 运行红测并确认失败原因**

```powershell
node --test test/attack-facing.test.js
```

Expected: 仅新测试失败；actual 约为 `-1.5707963267948966`，expected 为 `0`。若失败原因不同，停止并重新诊断。

- [ ] **Step 3: 实施最小修复**

在 `src/scene/systems.js` 中保留 `handlePlayerMovement()` 的方向记忆，只把 `tryStartDash()` 改为：

```js
// Dash toward the current movement input; fall back to the current facing.
let dashX = 0;
let dashY = 0;
if (this.keys.left.isDown) dashX -= 1;
if (this.keys.right.isDown) dashX += 1;
if (this.keys.up.isDown) dashY -= 1;
if (this.keys.down.isDown) dashY += 1;
if (dashX !== 0 || dashY !== 0) {
  this.dashAngle = Math.atan2(dashY, dashX);
} else {
  this.dashAngle = this.playerFacingAngle;
}
```

- [ ] **Step 4: 运行 focused 与完整回归**

```powershell
node --test test/attack-facing.test.js
node --test test/attack-facing.test.js test/attack-feedback-notification.test.js test/character-presentation.test.js test/presentation-rules.test.js
node --test
npm.cmd run build
git diff --check -- src/scene/systems.js test/attack-facing.test.js
```

Expected: focused、相关与全部 Node 测试通过；总数仍为 311；构建成功；diff check 无输出。

- [ ] **Step 5: 提交冲刺回归修复**

```powershell
git add -- src/scene/systems.js test/attack-facing.test.js
git diff --cached --check
git diff --cached --name-only
git commit -m "fix(gameplay): restore facing-based dash fallback"
```

Expected staged list: 仅上述两个文件。

---

### Task 3: 同步 v1.6 元数据、标准测试脚本与 CI

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `.github/workflows/ci.yml`
- Inspect only: `src/config/constants.js`

**Interfaces:**
- Consumes: cutoff 的 `1.3.0` 历史元数据和当前 main 的测试后构建 CI。
- Produces: `1.6.0` 元数据、`npm test` 公共接口、混合许可入口和继续保持关闭的 Debug 模式。

- [ ] **Step 1: 修改 package 元数据**

把 `package.json` 的相关字段改为：

```json
{
  "version": "1.6.0",
  "description": "2D 俯视角 Survivors-like 浏览器游戏，基于 Phaser 3 + Vite，包含正式像素素材、程序化 fallback 与合成音频。",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

保留其他字段和依赖版本不变。

- [ ] **Step 2: 让 npm 只同步 lockfile 根元数据**

先把 `package-lock.json` 顶层 `version` 与 `packages[""].version` 改为 `1.6.0`，然后运行：

```powershell
npm.cmd install --package-lock-only
git diff -- package-lock.json
```

Expected: lockfile 只反映根版本/根 package 元数据变化，不升级依赖、不重排无关节点。

- [ ] **Step 3: 让 CI 使用公共测试脚本**

保持现有 job 名 `Test and build` 与 Node 20，把 `.github/workflows/ci.yml` 的 Test step 改为：

```yaml
      - name: Test
        run: npm test
```

- [ ] **Step 4: 验证版本、脚本、Debug 和 CI**

```powershell
node -e "const p=require('./package.json');const l=require('./package-lock.json');if(p.version!=='1.6.0'||l.version!=='1.6.0'||l.packages[''].version!=='1.6.0'||p.scripts.test!=='node --test'||p.license!=='SEE LICENSE IN LICENSE'||p.description.includes('零素材'))process.exit(1)"
node --input-type=module -e "import { DEBUG_MODE } from './src/config/constants.js'; if (DEBUG_MODE !== false) process.exit(1)"
rg -n "name: Test and build|run: npm test|run: npm run build" .github/workflows/ci.yml
npm.cmd ci
npm.cmd test
npm.cmd run build
git diff --check
```

Expected: 三处版本一致；标准测试脚本存在；描述不再声称零素材；`DEBUG_MODE=false`；311 个测试和构建通过。

- [ ] **Step 5: 提交元数据与 CI**

```powershell
git add -- package.json package-lock.json .github/workflows/ci.yml
git diff --cached --check
git diff --cached --name-only
git commit -m "chore(release): prepare v1.6.0 metadata"
```

Expected staged list: 仅上述三个文件。

---

### Task 4: 建立可审查的许可、署名与素材登记

**Files:**
- Modify: `LICENSE`
- Create: `LICENSES/MIT.txt`
- Create: `LICENSES/CC-BY-SA-3.0.txt`
- Create: `ATTRIBUTION.md`
- Modify: `docs/licensing-and-commercialization.md`
- Modify: `docs/art/asset-register.md`

**Interfaces:**
- Consumes: 现有 MIT 文本、SCP 官方许可指南、SCP-049 官方 citation、Creative Commons legal code、53 项素材登记。
- Produces: 待 Level 2 确认的混合许可候选、公开署名、无账户绝对路径的来源登记。

- [ ] **Step 1: 保存现有 MIT 文本并加入官方 CC 文本**

使用 `apply_patch` 创建 `LICENSES/MIT.txt`，内容必须逐字等于修改前根 `LICENSE`。从以下官方固定 URL 读取 CC BY-SA 3.0 Unported legal code，并使用 `apply_patch` 将官方响应的完整正文写入 `LICENSES/CC-BY-SA-3.0.txt`；不得依赖浏览器展示行数或人工截取摘要：

```text
https://creativecommons.org/licenses/by-sa/3.0/legalcode.txt
```

验证：

```powershell
$originalMit = (git show HEAD:LICENSE) -join "`n"
$migratedMit = (Get-Content -Encoding utf8 LICENSES/MIT.txt) -join "`n"
if ($originalMit.TrimEnd() -ne $migratedMit.TrimEnd()) { throw 'MIT 文本迁移不一致' }
$officialCc = (Invoke-WebRequest -UseBasicParsing 'https://creativecommons.org/licenses/by-sa/3.0/legalcode.txt').Content -replace "`r`n", "`n"
$localCc = (Get-Content -Raw -Encoding utf8 LICENSES/CC-BY-SA-3.0.txt) -replace "`r`n", "`n"
if ($localCc -ne $officialCc) { throw 'CC BY-SA 3.0 正文与官方固定 URL 不一致' }
```

Expected: MIT 比较无差异；CC 文本首尾与官方 legal code 一致。

- [ ] **Step 2: 把根 LICENSE 改成范围总览**

根 `LICENSE` 使用清晰的中英文范围说明，必须包含以下事实：

```markdown
# SCP Survivor Licensing Overview / 许可总览

This repository contains materials under more than one license. 本仓库包含适用不同许可证的材料。

1. Independently authored software code / 独立软件代码
   Unless otherwise noted, independently authored software code remains available under the MIT License. The complete text is in `LICENSES/MIT.txt`.
2. SCP-derived content / SCP 衍生内容
   Content relating to the SCP Foundation, SCP-049, and other SCP-derived creative expression is provided under CC BY-SA 3.0, subject to the final project licensing review. The complete text is in `LICENSES/CC-BY-SA-3.0.txt`; attribution is in `ATTRIBUTION.md`.
3. Project-generated assets / 项目生成素材
   Source and modification records are listed in `docs/art/asset-register.md`. Inclusion here is not a warranty of uniqueness, non-infringement, or commercial-release clearance.

This overview preserves rights already granted for earlier MIT-licensed code copies and is not legal advice. 该总览不撤回既有 MIT 代码副本已经获得的权利，也不构成法律意见。
```

不得增加 NonCommercial、禁止再分发或其他与 CC BY-SA 冲突的限制。

- [ ] **Step 3: 创建公开 ATTRIBUTION**

`ATTRIBUTION.md` 必须按以下结构提供中英文事实：

```markdown
# Attribution / 署名

## SCP Foundation
SCP Survivor is based on content from the SCP Wiki and the SCP Foundation collaborative fiction project. SCP-related content is licensed under Creative Commons Attribution-ShareAlike 3.0.

- Source: https://scpwiki.com/
- License: https://creativecommons.org/licenses/by-sa/3.0/

## SCP-049
“SCP-049” was written by Gabriel Jade and rewritten by djkaktus & Gabriel Jade.

- Article: https://scpwiki.com/scp-049
- Official citation: “SCP-049” by Gabriel Jade, rewritten by djkaktus & Gabriel Jade, from the SCP Wiki.
- License: CC BY-SA 3.0
- Modification note: this project uses its own generated and edited game asset inspired by the article; it does not redistribute the article's source images.

## Project-generated visual assets / 项目生成视觉素材
The shipped pixel assets were generated and edited for this project without third-party image inputs according to the project asset register. The register records prompts, processing, dimensions, admission status, and remaining commercial-review risks.

See `docs/art/asset-register.md` and `docs/licensing-and-commercialization.md`. This file records project attribution and is not legal advice.
```

中文段落必须逐项表达相同事实，不能把 SCP-049 条目图片作者写成项目图片作者。

- [ ] **Step 4: 清理 asset register 的隐私路径与保留素材状态**

对 `docs/art/asset-register.md` 执行一次明确的机械替换：把全部 41 处

例如把实际登记值

```text
.codex\generated_images\019f5c42-ddb0-73d2-889d-cf86d3ad2300\exec-0ea14189-2748-4028-b8f5-50a152ba373b.png
```

改为：

```text
local-generation-archive:019f5c42-ddb0-73d2-889d-cf86d3ad2300/exec-0ea14189-2748-4028-b8f5-50a152ba373b.png
```

其余 40 处按同一规则转换。

保留 generation id、文件名、SHA-256 和接受/拒绝原因。表首新增统一声明：`.superpowers/...` 是不随 Release 分发的本地审计归档标识；运行时加载、公开署名和许可合规不得依赖该目录。

把 `infected-opening-sheet` 的 Admission 改为“R-17 roster 替换后停止 preload 的历史/溯源保留素材”，并在表首明确两个非运行时保留素材为：

- `infected-staff.png`
- `infected-opening-sheet.png`

不要复制、暂存或删除 UI worktree 的 `.superpowers/`。

- [ ] **Step 5: 重写许可政策中的错误事实**

`docs/licensing-and-commercialization.md` 必须：

- 把“当前代码只有根 MIT”改成候选混合许可结构，并说明既有 MIT 副本权利不撤回；
- 删除“尚无正式外部素材 / 当前只使用程序化 fallback”；
- 记录 53 个生产 PNG、51 个运行时素材与两个历史保留素材；
- 区分项目生成素材、SCP 衍生内容、第三方素材和未来商业复核；
- 链接根 `LICENSE`、`ATTRIBUTION.md`、两份完整许可文本和 `asset-register.md`；
- 明确项目所有者审批是项目门禁而非专业法律意见；
- 明确首次公开 push 前需要 Level 2 第二意见，仍有疑问时联系 SCP Licensing Team 或合格法律顾问。

- [ ] **Step 6: 验证许可与资产登记**

```powershell
$required = @('LICENSE','LICENSES/MIT.txt','LICENSES/CC-BY-SA-3.0.txt','ATTRIBUTION.md','docs/licensing-and-commercialization.md','docs/art/asset-register.md')
foreach ($path in $required) { if (-not (Test-Path -LiteralPath $path)) { throw "缺少 $path" } }
$publicDocs = @('LICENSE','ATTRIBUTION.md','docs/licensing-and-commercialization.md','docs/art/asset-register.md')
$hits = Select-String -Path $publicDocs -Pattern 'C:[\\/]+Users[\\/]+'
if ($hits) { $hits; throw '公开许可/素材文档仍包含账户专属绝对路径' }
$tracked = git ls-files -- '.superpowers/**'
if ($tracked) { $tracked; throw '.superpowers 被误纳入版本控制' }
$register = Get-Content -Raw -Encoding utf8 docs/art/asset-register.md
if ($register -notmatch '不随 Release 分发' -or $register -notmatch '运行时.*不得依赖') { throw 'asset register 缺少本地审计材料边界' }
rg -n "Gabriel Jade|djkaktus|CC BY-SA 3\.0|SCP Licensing Team|53|51|infected-opening-sheet" LICENSE ATTRIBUTION.md docs/licensing-and-commercialization.md docs/art/asset-register.md
git diff --check
```

Expected: 零账户路径；`.superpowers` 未跟踪；署名、许可和 53/51/2 边界可定位。

- [ ] **Step 7: 提交许可和登记候选**

```powershell
git add -- LICENSE LICENSES/MIT.txt LICENSES/CC-BY-SA-3.0.txt ATTRIBUTION.md docs/licensing-and-commercialization.md docs/art/asset-register.md
git diff --cached --check
git diff --cached --name-only
git commit -m "docs(license): define v1.6 attribution boundaries"
```

Expected staged list: 仅上述六个路径；提交信息不得称为“法律审查完成”。

---

### Task 5: 编写面向玩家的 v1.6 发布文案

**Files:**
- Create: `docs/releases/v1.6.0.md`
- Modify: `README.md`
- Modify: `README.en.md`
- Modify: `CHANGELOG.md`

**Interfaces:**
- Consumes: v1.6 实际视觉内容、v1.5 玩法基线、Task 4 的许可/署名入口。
- Produces: 面向访客的最新版本入口和可直接同步到 GitHub Release 的正文源。

- [ ] **Step 1: 创建 v1.6 Release 正文源**

`docs/releases/v1.6.0.md` 使用以下固定开场和结构：

```markdown
# v1.6.0 — 收容区焕新

基金会设施迎来完整的视觉升级。全新的标题界面、武器库、战术 HUD、设施场景、角色与异常敌人动画，让六分钟收容行动第一次拥有统一而清晰的视觉身份。

## 主要更新

- 标题与武器选择改造成统一的基金会设施终端和武器库。
- 新的战术 HUD 集中显示任务、设施、生命、武器、暂停与静音状态。
- 设施场景加入入口、战斗、维护和污染区域，并会随断电、事件和 Boss 阶段改变表现。
- 玩家、七类 R-17 敌人和 SCP-049 获得正式像素素材与动画表现。
- 命中、死亡、攻击、精英预警和异常材质获得更清晰的战斗反馈。
- 暂停、升级、失败和胜利界面统一为基金会终端风格。

<details>
<summary>开发与版本说明</summary>

- UI/Art 实现边界为 `31d8a240963b98f994d25c96414c9802a268ba0e`；其后的四个角色动画规划提交不属于本版本。
- 无输入冲刺继续跟随成功提交的当前攻击朝向，保持 v1.5 行为。
- 53 个生产 PNG 中有 51 个由运行时加载；程序化纹理和 Web Audio 继续作为 fallback 与动态表现。
- 完整 Node、Python 像素、构建、源码审查、浏览器 smoke 与项目所有者验收是发布门禁。
- SCP 衍生内容、SCP-049 和项目素材的许可与署名见 `LICENSE`、`ATTRIBUTION.md` 与 `docs/art/asset-register.md`。
- 本 Release 不包含橘色角色路线、120 帧角色路线、Electron、Windows 安装包或已核对的在线 Demo。

</details>
```

- [ ] **Step 2: 更新中英文 README 首屏**

中文 README：

- 许可徽章改为 `MIT + CC BY-SA 3.0` 并链接 `LICENSE`；
- 最新版本写成 `[v1.6.0 — 收容区焕新](./docs/releases/v1.6.0.md)`；
- 删除“UI 尚未发布”和“当前没有正式图片素材”；
- 用一段话说明正式像素素材已进入运行时，程序化纹理/音频仍作 fallback；
- Quick Start 增加 `npm test`；
- 当前包含列表补充正式设施、战术 HUD、玩家/R-17/SCP-049 表现；
- 许可段链接 `LICENSE`、`ATTRIBUTION.md` 和许可政策；
- 在线 Demo 继续明确标记为未核对是否等于 v1.6。

英文 README 使用自然对应名称 `v1.6.0 — Containment Zone Reforged`，逐项表达同一事实，不逐字硬译。

- [ ] **Step 3: 更新 CHANGELOG**

在 `Unreleased` 后加入：

```markdown
## [1.6.0] — 收容区焕新

### Added
- 生产化标题、武器库、战术 HUD、设施环境、角色/R-17/SCP-049 素材、终端覆盖层和池化战斗反馈。
- 53 个生产 PNG 的来源登记，其中 51 个由运行时 manifest 加载。
- 顶层许可总览、CC BY-SA 3.0 正文和 SCP/SCP-049 署名入口。

### Fixed
- 无输入冲刺恢复为跟随成功提交的当前攻击朝向，避免 UI 表现层方向记忆改变 v1.5 玩法语义。

### Changed
- 项目版本统一为 `1.6.0`，新增 `npm test`，CI 使用测试后构建流程。
- README、当前设计、项目状态和许可文档同步为正式素材已经进入运行时的事实。
```

文件末尾增加 `[1.6.0]` 到 `docs/releases/v1.6.0.md` 的链接；`Unreleased` 保持不包含尚未进入 `main` 的未来路线。

- [ ] **Step 4: 验证玩家文案边界**

```powershell
rg -n "v1\.6\.0|收容区焕新|Containment Zone Reforged|31d8a24|53|51|在线 Demo|Windows|橘色|120 帧|ATTRIBUTION" README.md README.en.md CHANGELOG.md docs/releases/v1.6.0.md
rg -n "没有正式图片|no production image|UI/Art.*未发布|UI/Art.*not released|License-MIT-yellow" README.md README.en.md
git diff --check
```

Expected: 第一条覆盖全部边界；第二条无输出；diff check 无输出。

- [ ] **Step 5: 提交玩家文案**

```powershell
git add -- README.md README.en.md CHANGELOG.md docs/releases/v1.6.0.md
git diff --cached --check
git diff --cached --name-only
git commit -m "docs: publish player-facing v1.6 copy"
```

Expected staged list: 仅上述四个文件。

---

### Task 6: 更新 main 权威事实与文档地图

**Files:**
- Modify: `docs/design.md`
- Modify: `docs/project-status.md`
- Modify: `docs/README.md`
- Inspect only: `docs/art-and-asset-direction.md`
- Inspect only: `docs/development-strategy.md`

**Interfaces:**
- Consumes: Tasks 1–5 的真实运行内容、版本、Release 和许可入口。
- Produces: 与候选版本相符的 `main` 当前事实、项目状态和文档入口。

- [ ] **Step 1: 更新当前游戏设计的表现事实**

`docs/design.md` 保留玩法、数值、六分钟时间轴和胜负描述不变，只更新 UI/资源章节：

- `PreloadScene` 从 manifest 加载 43 个静态图片和 8 个 spritesheet 条目；
- 51 个运行时正式素材包括玩家 opening sheet、七套 R-17 spritesheet、SCP-049、设施、菜单、武器、升级、终端和战斗反馈；
- `infected-staff` 与 `infected-opening-sheet` 是非运行时历史/溯源保留素材；
- 标题、武器库、五区战术 HUD、设施四分区、终端覆盖层、角色/敌人表现和池化战斗反馈已经实现；
- 程序化纹理仍为缺失素材 fallback 和动态效果，Web Audio 仍为程序化音频；
- 无输入冲刺沿当前攻击朝向，有输入时沿输入方向；
- “当前版本明确不包含”删除“正式图片、spritesheet”条目，只保留正式背景音乐/外部音效尚未包含。

- [ ] **Step 2: 更新当前项目状态**

`docs/project-status.md` 必须：

- 把稳定基线更新为本轮最终候选 `v1.6.0 — 收容区焕新`；
- 把 `31d8a24` UI/Art 纵切从候选改为本版本已集成范围；
- 保留 cutoff 后 4 个角色规划提交为未发布路线；
- 保留橘色路线和 App Platform 未发布；
- 记录 UI 分支/PR #3 不作为 v1.6 merge 入口；
- 在线 Demo 继续标为未核对；
- 清楚列出自动化、源码审查、视觉/试玩和许可门禁。

- [ ] **Step 3: 更新文档地图并复核策略文档**

`docs/README.md`：

- 在版本资料中加入 `releases/v1.6.0.md`；
- 在顶层入口加入 `../ATTRIBUTION.md` 与新的 `../LICENSE`；
- 说明 `docs/art/asset-register.md` 是正式素材来源登记；
- 不把历史计划或 `.superpowers/` 作为发布依赖。

只读复核 `docs/art-and-asset-direction.md` 与 `docs/development-strategy.md`。若其中混合资源和分支职责仍准确，不做无关修改；若出现“当前无正式素材”的明确错误，只做最小事实修订并把该文件加入本任务提交。

- [ ] **Step 4: 验证权威文档一致性**

```powershell
rg -n "v1\.6\.0|31d8a24|正式素材|51|53|player-opening-sheet|R-17|SCP-049|fallback|Web Audio|在线 Demo|角色动画|橘色|App Platform" docs/design.md docs/project-status.md docs/README.md docs/art-and-asset-direction.md docs/development-strategy.md
rg -n "manifest.*为空|没有正式图片|正式图片.*不包含" docs/design.md docs/project-status.md docs/README.md
git diff --check
```

Expected: 第一条能定位批准事实；第二条无当前事实误报；玩法和分支边界未扩张。

- [ ] **Step 5: 提交权威文档同步**

```powershell
git add -- docs/design.md docs/project-status.md docs/README.md
git diff --quiet -- docs/art-and-asset-direction.md
if ($LASTEXITCODE -ne 0) { git add -- docs/art-and-asset-direction.md }
git diff --quiet -- docs/development-strategy.md
if ($LASTEXITCODE -ne 0) { git add -- docs/development-strategy.md }
git diff --cached --check
git diff --cached --name-only
git commit -m "docs: align current facts with v1.6"
```

Expected: 只包含本任务列出的权威文档。

---

### Task 7: 运行完整本地发布门禁并生成 Level 2 审查包

**Files:**
- Inspect: complete candidate worktree
- Update later: `docs/superpowers/plans/2026-07-18-v1.6-integration-and-release.md`

**Interfaces:**
- Consumes: Tasks 1–6 的最终本地候选 HEAD。
- Produces: 可供源码审查、浏览器 smoke 和项目所有者判断的自动化/许可证据；不执行远端写操作。

- [ ] **Step 1: 验证版本、Debug、分支边界与资产覆盖**

```powershell
node -e "const p=require('./package.json');const l=require('./package-lock.json');if(p.version!=='1.6.0'||l.version!=='1.6.0'||l.packages[''].version!=='1.6.0'||p.scripts.test!=='node --test'||p.license!=='SEE LICENSE IN LICENSE')process.exit(1)"
if ($LASTEXITCODE -ne 0) { throw '版本、测试脚本或许可元数据不匹配' }
node --input-type=module -e "import { DEBUG_MODE } from './src/config/constants.js'; if (DEBUG_MODE !== false) process.exit(1)"
if ($LASTEXITCODE -ne 0) { throw 'DEBUG_MODE 必须为 false' }
node --input-type=module -e "import fs from'node:fs';import{IMAGE_ASSETS,SPRITESHEET_ASSETS}from'./src/assets/manifest.js';import{execFileSync}from'node:child_process';const loaded=[...IMAGE_ASSETS,...SPRITESHEET_ASSETS].map(x=>'public/'+x.path);const png=execFileSync('git',['ls-files','public/assets/art/**/*.png'],{encoding:'utf8'}).trim().split(/\r?\n/).filter(Boolean);const extra=png.filter(x=>!loaded.includes(x)).sort();if(png.length!==53||loaded.length!==51||JSON.stringify(extra)!==JSON.stringify(['public/assets/art/characters/infected-opening-sheet.png','public/assets/art/characters/infected-staff.png'])||loaded.some(x=>!fs.existsSync(x))){console.error({png:png.length,loaded:loaded.length,extra});process.exit(1)}"
if ($LASTEXITCODE -ne 0) { throw '53/51/2 素材边界不匹配' }
$excluded = @('da8b2354d5ab8344c2a873219d6bee992eb914d4','7767846b5007dc96b3cc03ba9832e57a3a9f0cbe','4a5512ffcdccff37c72541c58cd82f56594db468','68c23bc67134fe2e34a086217d639496320f8f25')
git merge-base --is-ancestor 31d8a240963b98f994d25c96414c9802a268ba0e HEAD
if ($LASTEXITCODE -ne 0) { throw 'cutoff 不在候选分支' }
foreach ($commit in $excluded) {
  git merge-base --is-ancestor $commit HEAD
  if ($LASTEXITCODE -eq 0) { throw "排除提交误入候选分支：$commit" }
  if ($LASTEXITCODE -ne 1) { throw "无法验证排除提交 ancestry：$commit" }
}
git rev-parse --verify HEAD | Out-Null
if ($LASTEXITCODE -ne 0) { throw '无法验证候选 HEAD' }
```

Expected: 版本、Debug、53/51/2 素材边界与 Git ancestry 全部匹配。

- [ ] **Step 2: 验证全部本地 Markdown 链接**

```powershell
$previousErrorActionPreference = $ErrorActionPreference
$ErrorActionPreference = 'Stop'
try {
function Get-MarkdownLinkTargets {
  param(
    [AllowEmptyCollection()][AllowEmptyString()][string[]]$Lines = @(),
    [Parameter(Mandatory = $true)][string]$Source
  )
  $inFence = $false
  $fenceChar = $null
  $fenceLength = 0
  $scanLines = foreach ($line in $Lines) {
    $fenceMatch = [regex]::Match($line, '^\s*(`{3,}|~{3,})(.*)$')
    if ($fenceMatch.Success) {
      $candidateFence = $fenceMatch.Groups[1].Value
      if (-not $inFence) {
        $inFence = $true
        $fenceChar = $candidateFence[0]
        $fenceLength = $candidateFence.Length
        continue
      }
      if (
        $candidateFence[0] -eq $fenceChar -and
        $candidateFence.Length -ge $fenceLength -and
        -not $fenceMatch.Groups[2].Value.Trim()
      ) {
        $inFence = $false
        $fenceChar = $null
        $fenceLength = 0
        continue
      }
    }
    if (-not $inFence) {
      [regex]::Replace($line, '`+[^`]*`+', 'INLINE_CODE')
    }
  }
  if ($inFence) {
    throw "$Source -> unclosed fenced code block"
  }
  $text = [regex]::Replace(($scanLines -join "`n"), '(?s)<!--.*?-->', '')
  foreach ($match in [regex]::Matches($text, '\[[^\]]*\]\(([^)]+)\)')) {
    $match.Groups[1].Value
  }
  foreach ($match in [regex]::Matches($text, '(?m)^\s*\[(?!\^)[^\]]+\]:\s*(?:<([^>]+)>|(\S+))')) {
    if ($match.Groups[1].Success) {
      $match.Groups[1].Value
    } else {
      $match.Groups[2].Value
    }
  }
}

$fixtureLines = @(
  '[`code`](missing-code.md)',
  '[release]: missing-reference.md',
  '```powershell',
  '[int](ignored-fenced.md)',
  '```',
  '`[char](ignored-inline.md)`'
)
$fixtureTargets = @(Get-MarkdownLinkTargets -Lines $fixtureLines -Source '<fixture>')
if ([string]::Join('|', $fixtureTargets) -ne 'missing-code.md|missing-reference.md') {
  $fixtureTargets
  throw 'Markdown 链接检查器负向 fixture 失败'
}

$files = @(git ls-files '*.md') + @('LICENSE')
if ($LASTEXITCODE -ne 0) { throw '无法枚举 Markdown 文件' }
$broken = foreach ($path in $files) {
  $file = Get-Item -LiteralPath $path
  $targets = @(Get-MarkdownLinkTargets -Lines (Get-Content -LiteralPath $path -Encoding utf8) -Source $path)
  foreach ($rawTarget in $targets) {
    $target = $rawTarget.Trim()
    if ($target -match '^<([^>]+)>') {
      $target = $Matches[1]
    } else {
      $target = ($target -split '\s+', 2)[0]
    }
    if ($target -match '^(https?://|mailto:|#)') { continue }
    $relative = ($target -split '#', 2)[0]
    if (-not $relative) { continue }
    $resolved = Join-Path $file.DirectoryName $relative
    if (-not (Test-Path -LiteralPath $resolved)) { "$path -> $target" }
  }
}
if ($broken) { $broken; throw 'broken local Markdown links' }
} finally {
  $ErrorActionPreference = $previousErrorActionPreference
}
```

Expected: 无输出、退出码 0。

- [ ] **Step 3: 验证隐私、许可和私有材料边界**

```powershell
$accountPathHits = @(git grep -n -I -i -E 'C:[\\/]+Users[\\/]' -- .)
$accountPathExit = $LASTEXITCODE
if ($accountPathExit -gt 1) { throw '无法扫描 tracked 账户路径' }
if ($accountPathHits) { $accountPathHits; throw 'tracked 文件仍包含账户专属绝对路径' }
$tracked = git ls-files -- '.superpowers/**' 'dist/**' 'node_modules/**'
if ($LASTEXITCODE -ne 0) { throw '无法检查私有或生成目录' }
if ($tracked) { $tracked; throw '私有或生成目录被误纳入版本控制' }
foreach ($pattern in @('Gabriel Jade','djkaktus','CC BY-SA 3\.0','SCP Licensing Team','SEE LICENSE IN LICENSE')) {
  rg -n -- $pattern LICENSE ATTRIBUTION.md docs/licensing-and-commercialization.md package.json
  if ($LASTEXITCODE -eq 1) { throw "缺少许可关键信息：$pattern" }
  if ($LASTEXITCODE -ne 0) { throw "无法检查许可关键信息：$pattern" }
}
```

Expected: 全部 tracked 文本零账户路径、零私有目录、许可关键信息全部可定位。

- [ ] **Step 4: 执行最终自动化**

```powershell
npm.cmd ci
if ($LASTEXITCODE -ne 0) { throw 'npm ci 失败' }
npm.cmd test
if ($LASTEXITCODE -ne 0) { throw 'Node 测试失败' }
$python = (Get-Command python -ErrorAction SilentlyContinue).Source
if (-not $python -or $python -match '\\WindowsApps\\') {
  $python = Join-Path ([Environment]::GetFolderPath('UserProfile')) '.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
}
if (-not (Test-Path -LiteralPath $python)) { throw '找不到可执行的 Python runtime' }
& $python scripts/art/test_pixel_tools.py
if ($LASTEXITCODE -ne 0) { throw 'Python 像素测试失败' }
npm.cmd run build
if ($LASTEXITCODE -ne 0) { throw 'Vite 构建失败' }
git diff --check main...HEAD
if ($LASTEXITCODE -ne 0) { throw 'main...HEAD diff check 失败' }
$status = git status --porcelain
if ($LASTEXITCODE -ne 0) { throw '无法读取 Git 状态' }
if ($status) { $status; throw '候选 worktree 不干净' }
git status --short --branch
if ($LASTEXITCODE -ne 0) { throw '无法显示 Git 状态' }
```

Expected: 314/314 Node 测试通过；Python 测试通过；构建成功；仅允许既有大 chunk warning；diff check 无输出；工作树干净。

- [ ] **Step 5: 生成不超过 800 字的 Level 2 外部 GPT 审查包**

在对话中生成以下七节，不伪称已发送或已获得意见：

```markdown
# GPT Review Request
## 1. 需要决定的问题
MIT 独立代码 + CC BY-SA 3.0 SCP 衍生内容的候选结构，是否足以用于本项目公开 GitHub 源码 Release；还需哪些修改或专业确认？
## 2. 当前背景
项目已有 MIT 历史版本；v1.6 首次加入 53 个正式 PNG，其中 SCP-049 是项目生成的衍生视觉。
## 3. 已确认事实
列出 SCP 官方游戏许可提醒、SCP-049 citation、OpenAI 输出条款和未使用条目原图。
## 4. 可选方案
候选混合结构、整体 CC BY-SA、GPLv3/专业意见三种路线及回退成本。
## 5. 当前 Agent 的建议
先保留 MIT 历史权利，公开发布前确认候选混合结构，仍有疑问则联系 SCP Licensing Team/法律顾问。
## 6. 希望 GPT 回答
最多三个关于兼容性、署名充分性和公开分发风险的问题。
## 7. 关键附件
`LICENSE`、`ATTRIBUTION.md`、`docs/licensing-and-commercialization.md`、asset register 摘要。
```

Expected: 总正文不超过 800 字；它只是第二意见请求，不替代法律意见。首次公开 push 前必须由项目所有者处理该门禁。

---

### Task 8: 对最终候选 HEAD 做独立源码审查与修复循环

**Files:**
- Review: `main...HEAD` complete diff and history
- Modify: only files required by accepted review findings

**Interfaces:**
- Consumes: Task 7 通过全部本地门禁的候选 HEAD。
- Produces: 获得最终源码审查批准且没有审查后未复核修改的 HEAD。

- [ ] **Step 1: 调用源码审查 skill**

调用 `superpowers:requesting-code-review`，审查范围必须包括：

- cutoff 和后 4 个提交边界；
- Scene shutdown/restart、manifest/fallback、物理 body 和战斗通知链；
- 数值、AI、存档、奖励、胜负和六分钟流程是否保持；
- dash TDD 是否真实覆盖已批准语义；
- 许可、README、Release、当前设计和 asset register 是否互相一致；
- `.superpowers`、本机路径、构建输出和私有材料是否泄漏。

- [ ] **Step 2: 核实并处理审查意见**

对每条意见先用真实代码/测试验证。明确错误或超范围建议不盲目实现；有效问题按 `superpowers:receiving-code-review` 处理。每个修复先加/调整失败测试，再最小修复并独立提交。

- [ ] **Step 3: 重跑 final-head 门禁并再次审查**

从候选 worktree **完整逐字重跑 Task 7 Step 1–4**：版本/Debug/53-51-2 资产边界、cutoff 与排除提交 ancestry、全部本地 Markdown 链接、账户路径与私有目录、许可关键词、`npm ci`、314 个 Node 测试、Python 像素测试、Vite 构建、`git diff --check main...HEAD` 和最终 Git 状态；不得只运行测试与构建子集。

Expected: 全部门禁通过、工作树干净。若审查后有任何已跟踪文件修改（包括代码、许可、资产登记或文档），提交修复后重新调用源码审查并再次完整执行 Task 7 Step 1–4，直到最终 HEAD 明确获批。

---

### Task 9: 启动稳定试玩服务并完成浏览器 smoke

**Files:**
- Inspect: built candidate application
- Temporary logs only: a dedicated directory under Windows temporary storage

**Interfaces:**
- Consumes: Task 8 审查通过的最终候选 HEAD。
- Produces: 由候选 worktree 所有的稳定 localhost URL、Agent smoke 证据和项目所有者验收入口。

- [ ] **Step 1: 启动 worktree-owned 固定端口服务**

优先使用 `64126`。先检查监听者；若端口被未知进程占用，选择新的空闲高位端口，绝不终止未知进程。

```powershell
$port = 64126
$listener = Get-NetTCPConnection -LocalPort $port -State Listen -ErrorAction SilentlyContinue
if ($listener) { throw "端口 $port 已被占用；选择新的空闲端口，不终止现有进程" }
$node = (Get-Command node).Source
$viteScript = (Resolve-Path -LiteralPath 'C:\scp-survivor-workspaces\releases\v1.6\node_modules\vite\bin\vite.js').Path
$runId = Get-Date -Format 'yyyyMMdd-HHmmss'
$smokeLogDir = Join-Path ([System.IO.Path]::GetTempPath()) "scp-survivor-v16-smoke-$runId"
New-Item -ItemType Directory -Force -Path $smokeLogDir | Out-Null
$process = Start-Process -FilePath $node -ArgumentList @($viteScript,'--host','127.0.0.1','--port',"$port",'--strictPort') -WorkingDirectory 'C:\scp-survivor-workspaces\releases\v1.6' -WindowStyle Hidden -RedirectStandardOutput (Join-Path $smokeLogDir 'vite.out.log') -RedirectStandardError (Join-Path $smokeLogDir 'vite.err.log') -PassThru
$listener = $null
for ($attempt = 0; $attempt -lt 120; $attempt += 1) {
  $listener = Get-NetTCPConnection -LocalPort $port -State Listen -ErrorAction SilentlyContinue
  if ($listener) { break }
  if ($process.HasExited) { throw "Vite 提前退出，检查 $smokeLogDir" }
  Start-Sleep -Milliseconds 250
}
if (-not $listener) { throw "Vite 在 30 秒内未监听端口 $port，检查 $smokeLogDir" }
if ($listener.OwningProcess -ne $process.Id) { throw '监听 PID 不属于本次 Vite 进程' }
$commandLine = (Get-CimInstance Win32_Process -Filter "ProcessId=$($process.Id)").CommandLine
if ($commandLine -notmatch 'scp-survivor-release-v1\.6' -or $commandLine -notmatch "$port") { throw '进程命令行不属于候选 worktree' }
$response = $null
for ($attempt = 0; $attempt -lt 40; $attempt += 1) {
  try { $response = Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port/"; break } catch { Start-Sleep -Milliseconds 250 }
}
if (-not $response -or $response.StatusCode -ne 200) { throw '候选 URL 在 10 秒内不可用' }
```

后续 PowerShell 调用或对话不能依赖 `$process` 变量。每次交付、复查或停止服务前，都从端口重新发现 PID，并用命令行验证 worktree 归属：

```powershell
$port = 64126
$listener = Get-NetTCPConnection -LocalPort $port -State Listen -ErrorAction Stop
$smokePid = $listener.OwningProcess
$commandLine = (Get-CimInstance Win32_Process -Filter "ProcessId=$smokePid").CommandLine
if ($commandLine -notmatch 'scp-survivor-release-v1\.6' -or $commandLine -notmatch "--port\s+$port") { throw '监听进程不属于 v1.6 候选 worktree' }
$response = Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port/"
if ($response.StatusCode -ne 200) { throw '候选 URL 不可用' }
```

如果实际选择了其他空闲端口，后续块必须使用同一个明确端口值；不得扫描后终止未知 Node 进程。

- [ ] **Step 2: 使用浏览器 skill 完成 Agent smoke**

调用 `browser:control-in-app-browser`，在最终 `DEBUG_MODE=false` 的候选版本执行：

- 960×540 标准画面和一个不同 viewport；
- 标题 → 武器选择 → 正式任务；
- WASD、冲刺、暂停、静音、TAB、升级选择/重抽/跳过；
- 断电、事件横幅、精英预警、七类 R-17、SCP-049 和战斗反馈；
- 至少两次 restart；
- 网络资源、浏览器控制台、缺失纹理和 fallback 警告。

终局状态只能通过公开控件验证，不调用 Scene 方法、不改运行时对象、不打开已提交调试开关：

1. **失败样本：** 在 960×540 画面从标题进入武器库，选择勤务手枪并开始任务；保持静止或主动靠近敌群且不闪避。若升级覆盖层冻结游戏，只点击可见卡片或“跳过”继续，直到正常伤害路径使生命归零。记录“行动终止”结果页与最终时间，点击“返回行动准备”并确认回到标题，作为第一次 restart。
2. **六分钟/Boss/胜利样本：** 从标题重新进入，选择收容突破器（霰弹枪）。使用 WASD 沿设施外围持续顺时针绕场，近身包围时按 SPACE；及时处理每次升级，优先伤害、射速、弹丸数和生存项。让 **游戏内** 计时真实到 `360.0s`，再等待正常 `2.8s` Boss 入场；记录常规刷怪停止、Boss 横幅和 SCP-049 出现。保持在霰弹枪约 230 范围内绕场攻击并闪避，直到正常 Boss 死亡路径显示“重新收容确认”；点击“返回行动准备”回到标题，作为第二次 restart。
3. 每个样本都记录候选 HEAD、URL、开始/结束时间、游戏内计时、结果页截图、控制台、网络和缺图情况。代码没有公开 E2E/DOM 测试入口，Phaser 内容位于 canvas，随机刷怪与升级也使固定坐标脚本不能保证胜利；Agent 可用浏览器/计算机控制执行正常公开操作，但完整六分钟胜利证据允许由项目所有者人工完成。
4. 不得使用调试键 `B`、DevTools、Scene 方法、计时/生命/伤害/Boss 状态篡改、浏览器运行时注入或临时 Debug 构建作为通过证据。若正常胜利样本未成功，允许重新正常游玩；在真正取得胜利证据前，Task 9 保持未通过。

Expected: 无控制台错误、无缺失生产素材、无点击区域阻塞、无玩法语义变化，并得到真实失败、六分钟 Boss、真实胜利和两次 restart 证据。保存必要截图用于报告，但不把临时截图提交进仓库。

- [ ] **Step 3: 把稳定链接和清单交给项目所有者**

交付前再次执行 PID、命令行和 HTTP 检查。向项目所有者提供实际 URL 与以下验收项：标题、武器库、HUD、设施比例、角色/R-17/SCP-049、战斗反馈、暂停/TAB/升级、失败/胜利/restart、冲刺方向、缺图和控制台。

Expected: 项目所有者明确给出视觉/试玩通过或具体修改意见。没有该结论时，不进入公开 GitHub 阶段。

若 Agent smoke 或项目所有者反馈导致任何已跟踪文件修改，必须提交修复并完整返回 **Task 7 → Task 8 → Task 9**：重新跑全部 final-head 门禁、重新审查新的最终 HEAD、重新 smoke，并重新取得项目所有者视觉/试玩结论。旧 HEAD 的证据不得沿用。

---

## v1.6 候选修正：普通治疗拾取物泛化

依据：[设计规格](../specs/2026-07-18-medkit-rename-design.md)与[实施计划](2026-07-18-medkit-rename.md)。状态：已完成本地修正。

- 项目所有者确认该物品只是普通回血补给，公开名称改为“急救包 / Medkit”，内部契约改为 `medkit`。
- 4:00、60 HP、位置、碰撞、音效、HUD cue 和程序化图形保持不变。
- 当前 tracked tree 移除旧条目身份；历史 Git/标签/Release 不重写。
- 该玩家可见修正使旧 HEAD 的 final-head 审查和视觉验收失效，必须在新 HEAD 重跑 Task 7–9。
- 本修正只关闭一个具体条目缺口，总体 Level 2 许可门禁仍未完成。

### Task 10: 经门禁授权后完成 GitHub PR、main、标签与 Release

**Files:**
- GitHub branch/PR/release state
- Update: `docs/superpowers/plans/2026-07-18-v1.6-integration-and-release.md` checkboxes

**Interfaces:**
- Consumes: 自动化、Level 2 许可选择、源码审查、Agent smoke 和项目所有者视觉验收均通过的最终 HEAD。
- Produces: 经独立授权的 v1.6 PR、main merge、`v1.6.0` tag 和 Latest GitHub Release。

- [ ] **Step 1: 展示最终证据并取得 push/PR 授权**

```powershell
git fetch origin --prune
git status --short --branch
git log --reverse --oneline main..HEAD
git diff --stat main...HEAD
git diff --check main...HEAD
gh pr list --state all --head codex/release-v1.6 --json number,title,state,isDraft,url
```

只有项目所有者明确授权后才能 push 并创建新 Draft PR；现有 PR #3 保持 Draft，不自动关闭或改写。

- [ ] **Step 2: push 并创建 Draft PR**

```powershell
git push -u origin codex/release-v1.6
$prBodyPath = Join-Path ([IO.Path]::GetTempPath()) 'scp-survivor-v16-pr-body.md'
# 主 Agent 先解析该绝对路径，再用 apply_patch 写入已展示并获准的最终 PR 正文。
if (-not (Test-Path -LiteralPath $prBodyPath)) { throw "缺少 PR 正文：$prBodyPath" }
gh pr create --draft --base main --head codex/release-v1.6 --title "release: v1.6.0 收容区焕新" --body-file $prBodyPath
$prNumber = [int](gh pr view codex/release-v1.6 --json number --jq '.number')
```

PR 正文必须列出玩家变化、cutoff、dash 修复、314 个测试、Python/构建结果、许可边界、视觉验收和明确排除项。临时正文文件放在 Windows 临时目录，不提交仓库。

- [ ] **Step 3: 检查 PR final-head CI 和 review**

```powershell
if (-not $prNumber) { $prNumber = [int](gh pr view codex/release-v1.6 --json number --jq '.number') }
gh pr checks $prNumber --watch
gh pr view $prNumber --json headRefOid,baseRefOid,isDraft,mergeable,mergeStateStatus,statusCheckRollup,url
```

Expected: Test and build 成功；HEAD OID 等于本地候选 HEAD；没有未解决的有效审查问题。任何改动后必须重跑 Task 7–9 的 final-head 门禁和项目所有者视觉验收。

- [ ] **Step 4: 分别取得 ready/merge 授权并合并**

先展示 PR、最终 HEAD、CI、审查和验收。取得 **ready 授权** 后执行：

```powershell
if (-not $prNumber) { $prNumber = [int](gh pr view codex/release-v1.6 --json number --jq '.number') }
$expectedHead = git rev-parse HEAD
$remoteHead = gh pr view $prNumber --json headRefOid --jq '.headRefOid'
if ($remoteHead -ne $expectedHead) { throw 'PR HEAD 与已验收候选 HEAD 不一致' }
gh pr ready $prNumber
gh pr checks $prNumber --watch
```

再次展示 ready 后的 CI 和精确 `$expectedHead`。只有取得独立 **merge 授权** 后才执行：

```powershell
$prNumber = [int](gh pr view codex/release-v1.6 --json number --jq '.number')
$expectedHead = git rev-parse HEAD
$remotePr = gh pr view $prNumber --json headRefOid,isDraft,statusCheckRollup | ConvertFrom-Json
if ($remotePr.headRefOid -ne $expectedHead) { throw 'PR HEAD 与已验收候选 HEAD 不一致' }
if ($remotePr.isDraft) { throw 'PR 仍是 Draft，停止 merge' }
gh pr checks $prNumber --watch
gh pr merge $prNumber --merge --match-head-commit $expectedHead
$prState = gh pr view $prNumber --json state --jq '.state'
if ($prState -ne 'MERGED') { throw "PR 尚未完成合并，当前状态：$prState" }
```

不得使用 `--admin`、`--auto`、force、rebase、squash 或 `--delete-branch`，也不得自动关闭/改写 PR #3。

- [ ] **Step 5: 在 main 上重新验证并取得 tag 授权**

更新本地 `main` 到远端合并结果：

```powershell
$prNumber = [int](gh pr view codex/release-v1.6 --json number --jq '.number')
git -C 'C:\scp-survivor' fetch origin --prune
if (git -C 'C:\scp-survivor' status --porcelain) { throw 'main 工作树不干净' }
git -C 'C:\scp-survivor' merge --ff-only origin/main
$mergedOid = gh pr view $prNumber --json mergeCommit --jq '.mergeCommit.oid'
$mainOid = git -C 'C:\scp-survivor' rev-parse main
if ($mainOid -ne $mergedOid) { throw '本地 main 不是 PR 的最终 merge commit' }
git -C 'C:\scp-survivor' merge-base --is-ancestor 31d8a240963b98f994d25c96414c9802a268ba0e main
if ($LASTEXITCODE -ne 0) { throw 'cutoff 不在最终 main' }
```

在 `C:\scp-survivor` 重新运行 Task 7 Step 1–4 的全部门禁，并按 Task 9 用另一个明确空闲端口重新完成浏览器 smoke。随后展示 `$mainOid` 作为精确 tag 目标。只有取得 **标签授权** 后才执行：

```powershell
$mainOid = git -C 'C:\scp-survivor' rev-parse main
$remoteMainOid = git -C 'C:\scp-survivor' rev-parse origin/main
if ($mainOid -ne $remoteMainOid) { throw '标签授权后 main 与 origin/main 已不一致' }
$existingTag = git ls-remote --tags origin v1.6.0 'v1.6.0^{}'
if ($existingTag) { $existingTag; throw '远端 v1.6.0 已存在，禁止移动或覆盖' }
if (git -C 'C:\scp-survivor' tag --list v1.6.0) { throw '本地 v1.6.0 已存在，先停止并核对，禁止覆盖' }
git -C 'C:\scp-survivor' tag -a v1.6.0 $mainOid -m 'v1.6.0 — 收容区焕新'
if ((git -C 'C:\scp-survivor' rev-list -n 1 v1.6.0) -ne $mainOid) { throw '本地 tag 目标不正确' }
git -C 'C:\scp-survivor' push origin refs/tags/v1.6.0
```

不得移动既有 tag；若 push 失败，保留本地注释标签并准确报告。

- [ ] **Step 6: 单独取得 GitHub Release 授权并发布**

从 `docs/releases/v1.6.0.md` 移除首个 H1 和紧随空行生成临时正文。主 Agent 先解析 `$releaseBodyPath` 的绝对路径，再用 `apply_patch` 写入完整正文；不得用 shell 重定向创建文件。展示名称、正文、tag 目标、非草稿/非预发布/Latest 状态。取得独立 **Release 授权** 后执行：

```powershell
$releaseBodyPath = Join-Path ([IO.Path]::GetTempPath()) 'scp-survivor-v16-release-body.md'
if (-not (Test-Path -LiteralPath $releaseBodyPath)) { throw "缺少 Release 正文：$releaseBodyPath" }
gh release create v1.6.0 --verify-tag --title 'v1.6.0 — 收容区焕新' --notes-file $releaseBodyPath --latest
```

不附加 Windows 或其他二进制产物，不使用自动生成正文。

- [ ] **Step 7: 反查并记录最终状态**

```powershell
$mainOid = git -C 'C:\scp-survivor' rev-parse main
$releaseBodyPath = Join-Path ([IO.Path]::GetTempPath()) 'scp-survivor-v16-release-body.md'
if (-not (Test-Path -LiteralPath $releaseBodyPath)) { throw "缺少已批准的 Release 正文：$releaseBodyPath" }
$remoteTag = git ls-remote --tags origin 'refs/tags/v1.6.0^{}'
if (-not $remoteTag) { throw '远端缺少 v1.6.0 peeled tag' }
$remoteTagOid = ($remoteTag -split '\s+')[0]
if ($remoteTagOid -ne $mainOid) { throw '远端 v1.6.0 未指向最终 main' }
$release = gh release view v1.6.0 --json tagName,name,body,targetCommitish,isDraft,isPrerelease,publishedAt,url | ConvertFrom-Json
if ($release.tagName -ne 'v1.6.0' -or $release.name -ne 'v1.6.0 — 收容区焕新' -or $release.isDraft -or $release.isPrerelease) { throw 'Release 名称、tag 或公开状态不正确' }
$expectedBody = (Get-Content -Raw -Encoding utf8 $releaseBodyPath) -replace "`r`n", "`n"
$actualBody = $release.body -replace "`r`n", "`n"
if ($actualBody.TrimEnd() -ne $expectedBody.TrimEnd()) { throw 'Release 正文与批准的临时正文不一致' }
$repo = gh repo view --json nameWithOwner --jq '.nameWithOwner'
$latestTag = gh api "repos/$repo/releases/latest" --jq '.tag_name'
if ($latestTag -ne 'v1.6.0') { throw "Latest Release 不是 v1.6.0，而是 $latestTag" }
if (-not $prNumber) { $prNumber = [int](gh pr view codex/release-v1.6 --json number --jq '.number') }
gh pr view $prNumber --json state,mergedAt,mergeCommit,url
git status --short --branch
```

Expected: tag 指向经验证的最终 main commit；Release 名称为 `v1.6.0 — 收容区焕新`，非草稿、非预发布、Latest；PR 已按批准方式合并；最终状态完整报告。

- [ ] **Step 8: 记录计划完成状态**

把实际完成的复选框改为 `[x]`，对仍受用户或外部门禁阻塞的步骤保持 `[ ]` 并写明证据，不伪造完成。使用独立文档提交记录最终计划状态；不得因此移动 tag 或修改 Release 历史。
