# U3 最终试玩验收与交付记录

2026-09-11，**用户已试玩，满意，暂时无修改意见**。这是用户对五页效果及最终交互的试玩验收，独立于下方自动化结果。本次只整理证据、验证候选并执行已授权 Git 集成，不继续改变视觉、玩法、数值或存档，不开始下一阶段。

[蓝图与实机对照](final-gallery.html) · [素材及许可](final-assets.md) · [QA 复现说明](qa.md) · [本次验证](evidence/closeout/final-validation.json)

## 验收冻结范围

- **升级选择**：灰绿工业设备、三张强化卡和专属图示；中文与真实前后数值实时显示。攻击节奏明确为“攻击间隔缩短”，显示实际封顶后的结果。金色只表示悬停/焦点；卡片点击立即应用，保留重抽 3 次、耗尽禁用、跳过最多恢复 8 生命、连续升级、一次性质变与不可撤销提示。覆盖步枪和 Tesla，不开放隐藏 shotgun。
- **当前构筑**：完整保留两把武器、单项强化次数、武器总等级、实际属性、协议及异常突变状态。长列表局部滚动，仍为只读。**按一下 Tab 暂停并打开构筑；松键保持；再按 Tab 或 Esc 关闭并继续。** 长按不连切，构筑期间时间、移动、攻击和冲刺停止；升级、普通暂停与结算不叠加构筑。此行为替代所有历史“按住查看、不暂停”的记录。
- **暂停**：紧凑工业面板、真实任务/时间/设施；继续行动为主操作，返回标题为次操作，沿用原有恢复与返回流程。
- **胜利结算**：设备与浅色纸质收容报告，“重新收容确认”“SCP-049 已重新收容”、印章及四项真实统计。
- **失败结算**：深色事故终端、红色中断状态和中断波形，保留“行动终止”、事故记录与四项统计；构图独立于胜利页。

结算继续读取生存时间、击杀、当局学分及累计学分；唯一主操作是“返回行动准备”，沿用 `scene.restart()`。奖励、防重复发奖、胜负、六分钟结构、永久加成及存档不变；Tesla 基础每跳 6 伤害／300 ms 不变。U2 HUD 文字层在覆盖、关闭及重启时正确显隐。PNG、DOM 或构筑创建失败仍保留已有回退与可恢复操作。

## 版本与证据来源

交付分支为 `codex/ui-art-u3-implementation`，实施基线为 `1ecb76839ca774aee4e87d98c35e867e79e6f7f6`。此前实机证据产生于该 HEAD 上的未提交 U3 工作版本，不能把基线哈希当作包含 U3 的提交。最终源码、测试、QA 与运行图在本次封包中冻结，见[验收代码指纹](evidence/closeout/accepted-source.json)。本记录随 U3 提交入库；实际集成哈希和 PR 以 Git 记录及收尾交付消息为准。

| 证据 | 对应版本及可支持的结论 |
| --- | --- |
| `evidence/tab-toggle/` | 2026-09-11 最终 Tab 暂停版本，用户随后试玩认可；本次没有改动其游戏源码。包含五页实机、两武器 DPR 1/2、升级/键盘/回退/连续重启、U1/U2 和生产包记录。其 `final-checks.json` 中 777 测试与“尚未发布”是集成前历史状态，不替代本次验证。 |
| `evidence/cadence/` | 2026-09-11 攻击节奏卡优化后、最终 Tab 改动前。只复用升级普通状态及 DPR 1.5 局部卡图、19 项节奏检查；不是新 Tab 行为证据。 |
| `evidence/refine/` | 2026-09-10 第三轮视觉版，早于节奏卡及新 Tab 改动。45 格显示矩阵只证明当时五页在 960×540、1440×810、1920×1080 和 DPR 1/1.5/2 下的几何、字体和溢出检查；额外状态与武器轮廓记录同属历史覆盖，不冒充最终版本完整矩阵。 |
| `evidence/closeout/` | 本次从明确暂存清单导出的干净候选验证。新的测试、生产构建、资源核验和引用检查结果在这里；不依赖工作区未提交历史资料。 |

所有选取 PNG 均来自实际运行的游戏；五张 `blueprints/` 原图单独标为概念。升级、结算、封顶、后期构筑及故障通过独立测试上下文构造，不声称自然通关。暂停截图包含真实键盘、指针和相机移动。开发测试使用 `49183` 临时上下文内的响应改写与内存场景构造；不把测试全局或入口写进产品。生产 smoke 使用 `49185` 独立临时上下文，真实输入、无响应改写或游戏状态注入，见[记录](evidence/tab-toggle/production-smoke.json)。没有操作用户浏览器 profile 或原存档。

历史[自然游玩记录](evidence/refine/natural-play.json)通过真实移动、攻击和拾取触发升级；脚本只读场景位置辅助移动，不设置伤害、生命、时间、等级或存档。这是短程游玩覆盖，不是自然六分钟通关记录。用户试玩验收来自本次用户直接反馈，不由这些自动化结果代替。

## 验证与审查

本次在暂存区导出的干净候选中重新执行 `npm ci`、`npm test` 和 `npm run build`，**777/777 测试通过，0 失败、0 跳过，生产构建通过（70 modules）**；`git diff --cached --check` 与 `git diff --check` 均通过。六张 U3 运行 PNG 与两张复用 U1 武器图在生产包中逐文件哈希匹配。新的生产 JS 与已验收试玩服务使用的 JS 字节一致，原实机记录可以按上方版本边界复用。本次结果见[收尾验证](evidence/closeout/final-validation.json)。[测试输出](evidence/closeout/npm-test.txt)、[构建输出](evidence/closeout/production-build.txt)、[生产素材核验](evidence/closeout/production-assets.json)和[候选引用检查](evidence/closeout/candidate-checks.json)随候选入库。独立封包审查检查了运行导入、六张 PNG、许可链、QA 前置条件与本次范围。

复用的实机交互证据包括：[Tab 暂停与三层回退](evidence/tab-toggle/tab-toggle-check.json)、[升级/构筑/结算](evidence/tab-toggle/interaction-results.json)、[键盘焦点](evidence/tab-toggle/keyboard-results.json)、[Esc 连续操作](evidence/tab-toggle/pause-sequence-debug.json)、[U1/U2 回归](evidence/tab-toggle/regression-results.json)和[攻击节奏专项](evidence/cadence/cadence-check.json)。

## 资料清单与保留方式

整理前 `docs/art/u3` 共 **486 文件、397,754,920 字节**。逐文件大小、SHA-256 与去向见[完整盘点 CSV](evidence-inventory.csv)。选择其中 **34 文件、18,555,664 字节**入库：五张蓝图、17 张实机截图和 12 份交互/验证 JSON。其余 **452 文件、379,199,256 字节**在原工作区原路径保留，不删除、不移动、不改写，包括初稿/第二轮/第三轮大部分矩阵图、旧报告、旧 gallery、性能实验和机器专属保全记录。

新建的最终文档、清单、候选日志与源码指纹另计；明确提交路径见[封包清单](evidence/closeout/package-manifest.json)。游戏新增六张运行图、八个 U3 模块、必要场景接入、11 个新增或更新测试文件、12 个 QA 脚本均包含在候选内。未使用的初稿外壳及六个历史 QA 实验脚本仅留本地。暂存使用逐文件清单，不整包暂存历史目录。

## 合理偏差与已知限制

蓝图示意的武器细节、生成字体与数字不逐像素复制；使用正式武器图和实时中文、真实封顶值。构筑多条强化时允许局部滚动。攻击节奏卡按用户试玩反馈强化了“间隔缩短”和实际等待时间的表达。以上效果已经本次用户试玩认可。

构建保留既有大 bundle 提示，首次显示可能有短暂绘制开销，没有稳定冷启动性能保证。本次 `npm ci` 对未变更依赖锁中的 nanoid、PostCSS 报告 2 项 high 审计提示；记录为既有依赖维护项，本轮未执行 audit fix 或扩大依赖升级。Windows 上的既有 LF/CRLF 提示不代表差异检查失败。历史显示矩阵与最终版本的边界如上，不将自动化通过当作所有机器的视觉保证。没有未解决的本次交付阻断。

本轮只按用户授权完成 commit、正常 push、PR 检查与合并、无 WIP 重叠时的本地 main 快进；保留现有分支和 worktree，不执行 tag、Release、部署或清理。
